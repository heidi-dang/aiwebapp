# Server runtime config
PORT=4001
# CORS origin for the UI (dev)
CORS_ORIGIN=http://localhost:4000
# Alternative UI dev port (registration page on /register)
# CORS_ORIGIN=http://localhost:3006
# Optional: require bearer auth when set
# OS_SECURITY_KEY=your_token_here
# Optional: enable SQLite persistence when set
# SQLITE_PATH=/absolute/path/to/agentos.sqlite

# Runner service base URL
RUNNER_URL=http://localhost:4002

# OAuth social login (optional)
# Public base URL for this server as seen by the browser (needed for OAuth redirect_uri when behind a tunnel)
# SERVER_PUBLIC_URL=https://api.heidiai.com.au
# OAUTH_STATE_SECRET=change_me
